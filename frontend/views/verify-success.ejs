<div class="card">
  <div class="success-icon">
    <svg width="64" height="64" viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
      <circle cx="32" cy="32" r="30" fill="#4CAF50" fill-opacity="0.1" stroke="#4CAF50" stroke-width="4"/>
      <path d="M20 32L28 40L44 24" stroke="#4CAF50" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  </div>
  
  <h1>Verified</h1>
  <p id="verification-message">You are pre-registered to participate in the "2024 Presidential Election - General Ballot."</p>
  
  <p class="notification">Once the election begins, you will receive a unique Voter ID number to email@gmail.com to begin your voting process.</p>
  
  <button id="home-btn" class="mt-20">Go to Dashboard</button>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const userRole = localStorage.getItem('userRole');
    
    // Update text based on user role
    if (userRole === 'host') {
      document.getElementById('verification-message').textContent = 
        'Your identity has been verified. You can now create and manage elections.';
    }
    
    // Set correct destination based on user role
    document.getElementById('home-btn').addEventListener('click', function() {
      // Redirect to template dashboard for testing
      window.location.href = '/template-dashboard';
    });
  });
</script>

<style>
  .success-icon {
    text-align: center;
    margin: 20px 0;
  }
  
  .notification {
    background-color: rgba(66, 103, 178, 0.1);
    border-left: 4px solid var(--accent-color);
    padding: 15px;
    margin: 20px 0;
    border-radius: 4px;
  }
</style> 